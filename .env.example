# Machine Shop Suite - 3D Printing Quote Engine Configuration
# Copy this file to .env and configure your settings

# =============================================================================
# PRUSASLICER CONFIGURATION
# =============================================================================

# Path to PrusaSlicer executable
# Linux/Mac (if in PATH): prusa-slicer
# Linux (custom install): /usr/local/bin/prusa-slicer
# Windows: C:\Program Files\Prusa3D\PrusaSlicer\prusa-slicer-console.exe
# Docker: prusa-slicer (installed via apt)
PRUSA_SLICER_PATH=prusa-slicer

# =============================================================================
# APPLICATION SETTINGS
# =============================================================================

# Flask environment (development/production)
FLASK_ENV=production

# Flask secret key (generate a random string for production)
# You can generate one using: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-secret-key-here-change-this-in-production

# Flask debug mode (True/False)
FLASK_DEBUG=False

# Application host and port
FLASK_HOST=0.0.0.0
FLASK_PORT=5000
